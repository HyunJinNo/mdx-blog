import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Archives",
  description: "Archives 페이지",
};

export default function Page() {
  return <main className="dark:bg-black">ARCHIVES</main>;
}
