export const metadata = {
  title: "pnpm + tsdown으로 React 라이브러리 만들기",
  description:
    "pnpm + tsdown으로 React 라이브러리를 만드는 방법에 대해 정리한 페이지입니다.",
  date: new Date("2026-03-26"),
  category: "Front-end",
  tags: ["monorepo", "nextra", "pnpm", "react", "tsdown"],
  pin: false,
  imagePath: "/images/front-end/front-end.avif",
};

> **<u>Tags</u>**
>
> monorepo, Nextra, pnpm, React, tsdown

> **<u>Environment</u>**
>
> pnpm v10.32.1
>
> nextra v4.6.1
>
> tsdown v0.21.3
>
> react v19.2.3
>
> \@tsdown/css v0.21.4

## 1. 개요

`pnpm` + `tsdown`으로 React 라이브러리를 만드는 방법에 대해 정리한 페이지입니다.

## 2. 프로젝트 구조 설계하기

React 라이브러리를 만들면서 **데모 앱을 통해 배포 없이 테스트**할 수 있도록
다음과 같이 `pnpm`을 사용한 모노레포 환경을 구성하고 프로젝트 구조를 설계합니다.

```plaintext
react-toc
├── apps
|   └── demo
|       ├── src
|       |   ├── app
|       |   ├── components
|       |   └── mdx-components.tsx
|       ├── (...)
|       ├── next.config.ts
|       ├── package.json
|       └── tsconfig.json
├── packages
|   └── react-toc
|       ├── src
|       |   ├── TocProvider.tsx
|       |   ├── (...)
|       |   └── index.ts
|       ├── package.json
|       ├── tsconfig.json
|       └── tsdown.config.ts
├── (...)
├── package.json
├── pnpm-lock.yaml
├── pnpm-worksplace.yaml
└── tsconfig.base.json
```

- `apps/demo`  
  Next.js 데모 앱으로, 개발하면서 바로 테스트할 환경입니다.
- `packages/react-toc`  
  `npm`에 배포할 실제 React 라이브러리입니다.

## 3. pnpm 설치하기

다음 명령어를 입력하여 `pnpm`을 설치합니다.

```bash
# pnpm 다운로드 및 설치
corepack enable pnpm

# pnpm 버전 확인
pnpm -v
```

## 4. 프로젝트 초기화하기

먼저 다음 명령어를 입력하여 프로젝트 루트를 생성합니다.

```bash
mkdir react-toc
cd react-toc
```

이후 다음 명령어를 입력하여 프로젝트를 초기화합니다.

```bash
pnpm init
```

생성된 `package.json` 파일에서 `"private": true`를 설정하여 루트 패키지의 `npm publish`를 방지합니다.

```json
{
  "name": "react-toc",
  "private": true,
  "packageManager": "pnpm@10.32.1"
}
```

## 5. pnpm workspace 설정하기

프로젝트 루트에서 `pnpm-workspace.yaml` 파일을 생성한 후, 다음과 같이 워크스페이스를 설정합니다.

```yaml
packages:
  - "apps/*"
  - "packages/*"
```

설정 이후 `apps/`, `packages/` 디렉토리 아래의 모든 패키지가 워크스페이스로 인식됩니다.

## 6. tsconfig.base.json 설정하기

각 라이브러리 패키지마다 `tsconfig.json` 설정을 하는 것은 번거로우므로,
다음과 같이 프로젝트 루트에 `tsconfig.base.json`을 생성하여 확장할 수 있도록 합니다.

먼저 프로젝트 루트에서 다음 명령어를 입력하여 TypeScript 컴파일러 설정 파일인 `tsconfig.json` 파일을 생성합니다.

```bash
tsc --init
```

이후 생성된 `tsconfig.json` 파일명을 `tsconfig.base.json`으로 변경한 후, 다음과 같이 설정합니다.

```json
{
  "compilerOptions": {
    /* Language and Environment */
    "target": "es2016" /* Set the JavaScript language version for emitted JavaScript and include compatible library declarations. */,
    "jsx": "react-jsx" /* Specify what JSX code is generated. */,

    /* Modules */
    "module": "esnext" /* Specify what module code is generated. */,
    "moduleResolution": "bundler" /* Specify how TypeScript looks up a file from a given module specifier. */,

    /* Emit */
    "declaration": true /* Generate .d.ts files from TypeScript and JavaScript files in your project. */,

    /* Interop Constraints */
    "esModuleInterop": true /* Emit additional JavaScript to ease support for importing CommonJS modules. This enables 'allowSyntheticDefaultImports' for type compatibility. */,
    "forceConsistentCasingInFileNames": true /* Ensure that casing is correct in imports. */,

    /* Type Checking */
    "strict": true /* Enable all strict type-checking options. */,

    /* Completeness */
    "skipLibCheck": true /* Skip type checking all .d.ts files. */
  }
}
```

## 7. React 라이브러리 생성하기

### 7.1. React 라이브러리 초기화하기

다음 명령어를 입력하여 React 라이브러리 폴더를 생성합니다.

```bash
mkdir -p packages/react-toc/src
cd packages/react-toc
```

이후 다음 명령어를 입력하여 라이브러리를 초기화합니다.

```bash
pnpm init
```

생성된 `package.json` 파일을 열고 다음과 같이 라이브러리를 배포하기 위한 설정을 진행합니다.

```json
{
  "name": "@hyunjinno/react-toc",
  "version": "0.0.1",
  "description": "A library that automatically generates a Table of Contents (TOC) from your headings with scroll tracking.",
  "type": "module",
  "main": "./dist/index.cjs",
  "types": "./dist/index.d.cts",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs"
    },
    "./style.css": "./dist/style.css"
  },
  "files": ["dist"],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsdown",
    "watch": "tsdown --watch ./src"
  },
  "keywords": ["toc", "table-of-contents"],
  "author": "HyunJinNo",
  "homepage": "https://hyunjinno.github.io/react-toc",
  "repository": {
    "type": "git",
    "url": "https://github.com/HyunJinNo/react-toc"
  },
  "license": "MIT",
  "peerDependencies": {
    "react": "^18 || ^19",
    "react-dom": "^18 || ^19"
  }
}
```

위의 `package.json` 설정 내용을 설명하면 다음과 같습니다.

#### 7.1.1. 패키지 기본 정보

```json
{
  "name": "@hyunjinno/react-toc",
  "version": "0.0.1",
  "description": "A library that automatically generates a Table of Contents (TOC) from your headings with scroll tracking."
}
```

- `name`  
  npm에 배포되는 패키지 이름으로, `@hyunjinno/...`와 같이 스코프를 포함시켰습니다.
  추후 라이브러리를 배포한 후, `npm install @hyunjinno/react-toc` 명령어로 설치할 수 있습니다.
- `version`  
  현재 라이브러리 버전을 나타냅니다.
- `description`  
  패키지 설명 부분입니다.

#### 7.1.2. 모듈 시스템 설정

```json
{
  "type": "module",
  "main": "./dist/index.cjs",
  "types": "./dist/index.d.cts"
}
```

- `type`  
  `"type": "module"`로 설정하면 기본을 `ESM(ES Module)`로 사용하게 되며,
  모든 `.js` 파일이 ESM으로 해석됩니다.
- `main`
  **CommonJS 환경(require)**에서의 진입점입니다.
- `types`
  TypeScript 타입 정의 파일을 지정하는 부분입니다.

#### 7.1.3. sideEffects

```json
{
  "sideEffects": false
}
```

Tree-shaking 최적화를 통해 사용하지 않는 코드를 제거할 수 있습니다.

#### 7.1.4. exports

```json
{
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs"
    },
    "./style.css": "./dist/style.css"
  }
}
```

exports 옵션을 통해 ESM과 CJS 모두 지원하도록 설정합니다.

| 상황          | 파일          |
| ------------- | ------------- |
| ESM (import)  | `index.mjs`   |
| CJS (require) | `index.cjs`   |
| 타입          | `index.d.mts` |

```javascript
import { Toc } from "@hyunjinno/react-toc";
```

```javascript
const { toc } = require("@hyunjinno/react-toc");
```

또한 CSS를 별도 export로 제공하도록 설정합니다.

```javascript
import "@hyunjinno/react-toc/style.css";
```

#### 7.1.5. files

```json
{
  "files": ["dist"]
}
```

`npm publish`시 **dist만 포함**하도록 설정합니다.

#### 7.1.7. scripts

```json
{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsdown",
    "watch": "tsdown --watch ./src"
  }
}
```

위에서 설정한 명령어레 대해 설명하면 다음과 같습니다.

- `build`  
  tsdown 번들러를 통해 dist를 생성합니다.
- `watch`  
  watch 모드로 실행하면 코드 수정 시 자동으로 재빌드됩니다.

#### 7.1.8. 메타 정보

```json
{
  "keywords": ["toc", "table-of-contents"],
  "author": "HyunJinNo",
  "homepage": "https://hyunjinno.github.io/react-toc",
  "repository": {
    "type": "git",
    "url": "https://github.com/HyunJinNo/react-toc"
  },
  "license": "MIT"
}
```

- `keywords`  
  npm에 표시될 키워드 목록을 지정하는 부분입니다.
- `author`  
  라이브러리 제작자를 지정하는 부분입니다.
- `homepage`  
  npm에 표시될 Homepage를 지정하는 부분으로,
  여기서는 GitHub Pages로 배포할 데모 웹사이트 URL을 작성하였습니다.
- `repository`  
  라이브러리 저장소를 지정하는 부분입니다.
- `license`  
  라이브러리 라이선스를 지정하는 부분입니다.

#### 7.1.9. peerDependencies

```json
{
  "peerDependencies": {
    "react": "^18 || ^19",
    "react-dom": "^18 || ^19"
  }
}
```

번들 중복 방지를 위해 peerDependencies에 React를 외부 의존성으로 지정합니다.
이를 통해 라이브러리 사용자가 React를 설치하게 됩니다.

### 7.2. tsconfig.json 설정하기

생성한 라이브러리 디렉토리에서 `tsconfig.json` 파일을 생성한 후,
프로젝트 루트에 있는 `tsconfig.base.json`을 확장하도록 설정합니다.

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "declaration": true,
    "declarationDir": "dist"
  },
  "include": ["src"]
}
```

### 7.3. React 라이브러리 의존성 설치하기

프로젝트 루트에서 다음 명령어를 입력하여 React 라이브러리 및 `tsdown` 번들러 의존성을 설치합니다.

```bash
pnpm add -Dw react react-dom @types/react @types/react-dom typescript tsdown @tsdown/css
```

특정 라이브러리에서만 필요한 의존성의 경우, 다음과 같이 설치할 수 있습니다.

```bash
pnpm add -D clsx
```

### 7.4. tsdown 설정하기

`packages/react-toc` 디렉토리에서 `tsdown.config.ts` 파일을 생성한 후, 다음과 같이 설정합니다.

```typescript
/* tsdown.config.ts */

import { defineConfig } from "tsdown";

export default defineConfig({
  entry: ["./src/index.ts"],
  format: ["esm", "cjs"],
  dts: true,
  clean: true,
  deps: { neverBundle: ["react", "react-dom"] },
});
```

위의 코드를 설명하자면 다음과 같습니다.

- `entry`  
  빌드 진입점을 설정합니다.
- `format`  
  ESM과 CJS 모두 지원하도록 설정합니다.
- `dts`  
  `.d.ts` 파일을 생성하도록 설정합니다.
- `clean`  
  라이브러리 빌드 전, 이전에 생성된 빌드 결과물을 삭제하도록 설정합니다.
- `deps`  
  React 번들을 제외하도록 설정합니다.

### 7.5. React 컴포넌트 생성하기

![React 컴포넌트 생성하기](/images/front-end/pnpm-tsdown-react-library/pic1.png)

```typescript
import clsx from "clsx";

type H2Props = Omit<
  React.ComponentProps<"h2"> & {
    children: string;
  },
  "id"
>;

export const H2 = ({ children, ...props }: H2Props) => {
  return (
    <h2 {...props} className={clsx("toc-heading", props.className)}>
      {children}
    </h2>
  );
};
```

### 7.6. style.css 생성하기

```css
.react-toc-wrapper {
  display: flex;
  flex-direction: column;
  gap: 1rem;
  border-left: 1px solid #dddddd;
  height: fit-content;
  padding-bottom: 1rem;
}

.react-toc-wrapper-heading {
  padding-left: 1rem;
  font-weight: 500;
}

.react-toc-list {
  display: flex;
  flex-direction: column;
  gap: 0.625rem;
}

.react-toc-link {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;

  &:hover {
    color: #0056b2;
  }
}

.react-toc-active {
  border-left: 1px solid #0056b2;
  color: #0056b2;
  margin-left: -1px;
}

.react-toc-h2 {
  padding-left: 1rem;
}

.react-toc-h3 {
  padding-left: 1.75rem;
}

.react-toc-h4 {
  padding-left: 2.5rem;
}

.react-toc-h5 {
  padding-left: 3.25rem;
}

.react-toc-h6 {
  padding-left: 4rem;
}
```

### 7.7. index.ts 설정하기

```typescript
import "./style.css";

export { H2 } from "./H2";
export { H3 } from "./H3";
export { H4 } from "./H4";
export { H5 } from "./H5";
export { H6 } from "./H6";
export { Toc } from "./Toc";
export { TocProvider } from "./TocProvider";
```

### 7.8. 라이브러리 빌드하기

다음 명령어를 입력하여 라이브러리 빌드를 수행할 수 있습니다.

```bash
pnpm run build
```

![라이브러리 빌드하기](/images/front-end/pnpm-tsdown-react-library/pic2.png)

또는 watch 모드로 실행하여 코스 수정 시 자동으로 재빌드할 수 있습니다.

```bash
pnpm run watch
```

## 8. Next.js 데모 앱 생성하기

### 8.1. demo 생성하기

먼저 `apps` 디렉토리를 생성합니다.

```bash
mkdir apps
cd apps
```

이후 다음 명령어를 입력하여 생성한 React 라이브러리를 테스트할 데모 앱을 생성합니다.

```bash
pnpm create next-app
```

![demo 생성하기](/images/front-end/pnpm-tsdown-react-library/pic3.png)

`apps/demo` 디렉토리에서 불필요한 `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `node_modules`를 삭제합니다.

### 8.2. Nextra 설치하기

#### 8.2.1. Nextra 의존성 설치하기

생성한 demo 앱에서는 라이브러리 문서를 작성할 예정이므로 `Nextra`를 설치하겠습니다.
`apps/demo` 디렉토리에서 다음 명령어를 입력하여 `Nextra`를 설치합니다.

```bash
pnpm add nextra nextra-theme-docs
```

#### 8.2.2. next.config.ts 설정하기

```typescript
import type { NextConfig } from "next";
import nextra from "nextra";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  output: "export",
  basePath: "/react-toc",
  images: {
    unoptimized: true,
  },
};

const withNextra = nextra({
  // ... other Nextra config options
});

export default withNextra({
  ...nextConfig,
  turbopack: {
    resolveAlias: {
      // Path to your `mdx-components` file with extension
      "next-mdx-import-source-file": "./src/mdx-components.tsx",
    },
  },
});
```

#### 8.2.3. mdx-components.tsx 생성하기

```typescript

```

## TODO. 참고 자료

- [React+typescript 컴포넌트 라이브러리 생성하기 ③rollup 설정 — 콩부합시다](https://codgehog.tistory.com/19?category=1187053)
- [고티켓-react 컴포넌트 라이브러리 만들기(3)-rollup — 이찬진 컴퓨터 교실](https://devnm.tistory.com/6)
- [esbuild vs rollup vs webpack vs tsup | "웹 자바스크립트 번들러" npm 패키지 비교](https://npm-compare.com/ko-KR/esbuild,rollup,tsup,webpack)
- [리액트 컴포넌트 라이브러리를 빌드하고 배포하는 방법 — React와 TypeScript를 좋아하는 개발자](https://xionwcfm.tistory.com/450)
- [리액트 라이브러리 npm에 배포하기 with rollup](https://jha-memo.tistory.com/228)
- [rolldown/tsdown: The elegant bundler for libraries powered by Rolldown](https://github.com/rolldown/tsdown)
- [Nextra – Next.js Static Site Generator](https://nextra.site/)
